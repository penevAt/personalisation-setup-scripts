Done
	moved scripts and all other config files

Important 
	add ~/bin to PATH in .bash_profile   -- DONE
	Specify directory of devCert.p12 file then generate certs  -- DONE
		generate certs
			openssl pkcs12 -nodes -in developerCertificate.p12 -out certificate.pem
			sudo mv certificate.pem /etc/pki/
			openssl pkcs12 -in developerCertificate.p12 -out client.crt -clcerts -nokeys
			openssl pkcs12 -in developerCertificate.p12 -out client.key -nocerts -nodes
			sudo mv ~/Documents/certs/client.crt /etc/pki/tls/certs/
			sudo mv ~/Documents/certs/client.key /etc/pki/tls/private/
	
	
	install prerequisites
		* install homebrew
		* install mvn
		* install pyhon + pyenv (https://gist.github.com/patriciogonzalezvivo/77da993b14a48753efda)
	
ADD console logging

Write a readme
	https://confluence.dev.bbc.co.uk/display/MyBBC/myBBC+New+Joiner+Induction+Pack
	https://confluence.dev.bbc.co.uk/display/mypers/Getting+Started

Later
	Also, please unselect 'SOCKS' configuration in proxy settings of your local MAC,if it is added by the configuration. This results not to download the required artefacts from artifactory..
	install of bbc Jonathan scripts(requires python to be setup) (also requires to be part of bbc)
	maybe install ruby
	look into other prerequisites
	maybe add option to setup OMZhs + THEME

